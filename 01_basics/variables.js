const pi = 3.14;
//Const keyword is used with variable which will not be changed in future and to avoid accidental change.
var firstName = "Hassan";
let lastName = "Khan";
/*var and let both are changable but difference between var and let is of scope. var keyword variable have I
think global scope which means it change in the variable value could result in unexpected outcomes while let 
keyword has block level scope (Ithink) which is why this keyword is recommended to use while declaring a 
variable
*/
console.log(pi);
//the above is used to print outputs on the screen
console.table([pi, firstName, lastName]);
// above is used to print outputs on the screen in tabular format!!
// this is all for the first video now on to the second video byyy!!!